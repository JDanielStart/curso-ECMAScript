let hello = "Hello";
let world = "World";
let epicPhrase = hello + " " + world + "!";
console.log(epicPhrase);

//Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

//Multi-line strings
let lorem = "esto es un string \n" + "esto es otra línea";

let lorem2 = `Esta es una frase èpica
  la continuación de una frase èpica`;

console.log(lorem);
console.log(lorem2);